# pydoc-markdown.yml
loaders:
  - type: python
    # Where to search for Python packages/modules
    search_path:
      - ./../wizard_tk_bridge/src
    # Packages (directories with __init__.py) to document
    packages:
      - wizard_tk_bridge

processors:
  - type: filter
    # Optional: skip private members, etc.
    expression: >
      (not name.startswith('_') or name in ('__init__', '__call__')) and
      default()
    skip_empty_modules: true
    exclude_private: true
  - type: smart
  - type: crossref

renderer:
  type: markdown
  filename: ./doc/wizard_tk_bridge_api.md
